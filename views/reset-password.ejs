<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reset Password</title>
    <link rel="stylesheet" href="/public/css/style1.css" />
    <style>
      /* Resetting some default styles */
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f0f0f0;
      }

      header {
        text-align: center;
        padding: 20px;
        background-color: #804000;
        color: beige;
      }

      header h1 {
        margin: 0;
        font-size: 2rem;
      }

      header p {
        color: beige;
      }

      /* Main Section */
      main {
        max-width: 400px;
        margin: 50px auto;
        background-color: #ffffff;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        padding: 20px 30px;
      }

      form label {
        font-weight: bold;
        margin-bottom: 8px;
        display: block;
      }

      form input {
        width: 100%;
        padding: 10px;
        margin-bottom: 15px;
        border: 1px solid #ccc;
        border-radius: 5px;
        box-sizing: border-box;
        font-size: 1rem;
      }

      form input:focus {
        border-color: #0066cc;
        outline: none;
      }

      button {
        width: 100%;
        padding: 12px;
        background-color: #804000; /* Matching banner color */
        color: white; /* Contrast color */
        font-size: 1rem;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s ease;
      }

      button:hover {
        background-color: #d9b99b; /* Slightly darker beige on hover */
      }

      footer {
        text-align: center;
        margin-top: 30px;
        padding: 10px;
        background-color: #f4f4f4;
        font-size: 0.9rem;
        color: #666;
      }
    </style>
  </head>
  <body>
    <header>
      <div class="logo">
        <img src="/public/images/logo4.png" alt="Khadibv Logo" />
        <h1>KhadiBV</h1>
        <p style="color: beige">Sustainable Design Powered by Banasthali</p>
      </div>
    </header>

    <main>
      <form action="/reset-password" method="POST">
        <!-- Hidden Email Field -->
        <input type="hidden" name="email" value="<%= email %>" />

        <!-- New Password -->
        <label for="new-password">New Password:</label>
        <input
          type="password"
          id="new-password"
          name="newPassword"
          required
          placeholder="Enter new password"
        />

        <!-- Confirm Password -->
        <label for="confirm-password">Confirm Password:</label>
        <input
          type="password"
          id="confirm-password"
          required
          placeholder="Confirm new password"
        />

        <!-- Reset Button -->
        <button type="submit">Reset Password</button>
      </form>
    </main>

    <footer>
      <p>&copy; 2024 Khadibv</p>
    </footer>

    <script>
      document.querySelector("form").addEventListener("submit", function (e) {
        const password = document.getElementById("new-password").value;
        const confirmPassword = document.getElementById("confirm-password").value;

        if (password !== confirmPassword) {
          e.preventDefault();
          alert("Passwords do not match.");
        }
      });
    </script>
  </body>
</html>
